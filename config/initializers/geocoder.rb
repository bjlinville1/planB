Geocoder::Configuration.timeout = 30
#to do: find out why geocoder times out at 30
#could be my API key failing, could be my proxy, could be the dns lookup(if so change dns servers). Run geocode -h in command line for config options